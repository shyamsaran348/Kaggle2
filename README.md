# Kaggle - Reddit Rule Violation Detection

This project trains models (Logistic Regression & XGBoost with hyperparameter tuning) to detect whether a Reddit post violates community rules.

## Folder Structure
